# Optional for live Responses API normalization, planning, and completion summaries.
OPENAI_API_KEY=
# Optional custom OpenAI-compatible endpoint.
OPENAI_BASE_URL=
# Optional for Linear polling and status sync.
LINEAR_API_KEY=
# Optional for GitHub pull request creation on remote repositories.
GITHUB_TOKEN=
# Optional log level override for local runs.
LOG_LEVEL=info
